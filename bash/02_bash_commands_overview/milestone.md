# Milestone - 02 Bash Commands Overview

## Chapter Completed

✅ Bash Commands Overview

---

## Learning Objectives Achieved

I can now:

- Explain the purpose of common Bash commands.
- Check my current working directory.
- List directory contents.
- Move between directories.
- Create directories.
- Create files.
- Display text in the terminal.
- Display the contents of files.
- Copy files.
- Move or rename files.
- Remove files.
- Combine basic commands into a simple workflow.

---

## Commands Practiced

```bash
pwd
ls
cd
mkdir
touch
echo
cat
cp
mv
rm
```

---

## Key Concepts Learned

**`pwd`** — Shows my current working directory.

**`ls`** — Lists the contents of a directory.

**`cd`** — Changes my current working directory.

**`mkdir`** — Creates a new directory.

**`touch`** — Creates an empty file.

**`echo`** — Produces or displays text.

**`cat`** — Displays the contents of a file.

**`cp`** — Copies files or directories.

**`mv`** — Moves or renames files or directories.

**`rm`** — Removes files.

---

## Practical Workflow

During this chapter I created a temporary practice directory and worked with files inside it. I:

1. Created a directory.
2. Entered the directory.
3. Created a file.
4. Added text to the file.
5. Displayed the file contents.
6. Copied the file.
7. Renamed the copy.
8. Removed the renamed file.

This helped me understand how common Bash commands can work together.

---

## Important Lesson

This chapter was intentionally an overview. I do not need to master every option or advanced feature of these commands yet. The purpose was to build a basic mental map of the commands and understand what each one does. The following chapters will explore the commands in greater depth.

---

## Challenges Faced

- Understanding the difference between `pwd` and `ls`.
- Understanding the difference between `cp` and `mv`.
- Understanding that `mv` can both move and rename.
- Understanding that `rm` removes files.
- Understanding how several commands can be combined into a workflow.

---

## How I Solved Them

I practiced each command directly in the terminal and observed the result. Instead of only memorizing definitions, I created files and directories, moved around, copied files, renamed files, and removed files.

---

## Command Map

```
pwd    → Where am I?
ls     → What's here?
cd     → Move somewhere
mkdir  → Create a directory
touch  → Create a file
echo   → Produce/display text
cat    → Display file contents
cp     → Copy files/directories
mv     → Move/rename files/directories
rm     → Remove files
```

---

## Skills Gained

After completing this chapter, I can:

- Navigate a Bash environment.
- Create basic files and directories.
- Inspect my current location.
- Inspect directory contents.
- Work with files using basic Bash commands.
- Perform simple file-management tasks.
- Combine commands to complete a basic workflow.

---

## Self-Assessment

| Skill | Confidence |
|---|---|
| Use `pwd` | ⭐⭐⭐⭐⭐ |
| Use `ls` | ⭐⭐⭐⭐⭐ |
| Use `cd` | ⭐⭐⭐⭐⭐ |
| Use `mkdir` | ⭐⭐⭐⭐⭐ |
| Use `touch` | ⭐⭐⭐⭐⭐ |
| Use `echo` | ⭐⭐⭐⭐⭐ |
| Use `cat` | ⭐⭐⭐⭐⭐ |
| Use `cp` | ⭐⭐⭐⭐☆ |
| Use `mv` | ⭐⭐⭐⭐☆ |
| Use `rm` | ⭐⭐⭐⭐☆ |
| Combine basic commands | ⭐⭐⭐⭐⭐ |

---

## Personal Reflection

This chapter gave me my first broad map of the everyday commands used in Bash. Instead of learning commands in isolation, I practiced them together in a small workflow. I now understand the basic purpose of the most common Bash commands and have a foundation for studying each command in greater depth.

---

## Next Step

The following chapters will explore these commands individually and introduce more advanced options and techniques. I will continue building my knowledge one command at a time.

