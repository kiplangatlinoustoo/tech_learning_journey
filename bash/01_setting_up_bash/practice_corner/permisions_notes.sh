#!/bin/bash

echo "This script only runs directly because it has execute permission."
echo "That permission was granted using: chmod +x permissions_notes.sh"
echo "Without it, ./permissions_notes.sh would return: Permission denied"
